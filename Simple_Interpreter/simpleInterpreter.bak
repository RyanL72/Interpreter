#lang racket

;;===============================================================
;; Simple Interpreter
;;
;; CSDS 345
;=======================================
; Interpret a file

(require "simpleParser.rkt")

(define return
  (lambda (expression)
    (expression)))